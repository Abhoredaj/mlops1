from setuptools import setup, find_packages

setup(
    name="src",
    version="0.0.1",
    description="its a Fatal health classification", 
    author="fahad", 
    packages=find_packages(),
    license="MIT"
)